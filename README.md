# Jae Ho Chang — academic site

Custom Jekyll site for GitHub Pages.

## Local preview

```bash
bundle exec jekyll serve
```

Open `http://localhost:4000`.

## Content sources

- CV / statements: `portfolio/` (copied into `files/` for the site)
- Portrait: `images/jae.jpg`